<%@ page contentType="text/html;charset=UTF-8" %><%@ taglib prefix="c" uri="jakarta.tags.core" %>
<c:set var="pageTitle" value="Pickup & Delivery" scope="request"/><jsp:include page="/WEB-INF/views/common/header.jsp"/>
<div class="page-actions"><div><h1 class="page-heading">Pickup & delivery</h1><p>Schedule collections and track garments to the customer.</p></div><c:if test="${sessionScope.role=='ADMINISTRATOR'||sessionScope.role=='DELIVERY_COORDINATOR'}"><a class="btn btn-primary" href="${pageContext.request.contextPath}/deliveries/new"><i class="bi bi-plus-lg"></i> New request</a></c:if></div>
<div class="panel"><form class="filter-bar" method="get" action="${pageContext.request.contextPath}/deliveries/"><select class="form-select" name="status"><option value="">All statuses</option><c:forEach items="${['REQUESTED','SCHEDULED','PICKED_UP','PROCESSING','OUT_FOR_DELIVERY','DELIVERED','CANCELLED']}" var="status"><option value="${status}" ${param.status==status?'selected':''}><c:out value="${status}"/></option></c:forEach></select><input class="form-control" name="date" type="date" value="<c:out value='${param.date}'/>"><button class="btn btn-outline-primary">Filter</button><a class="btn btn-light" href="${pageContext.request.contextPath}/deliveries/">Reset</a></form>
<c:choose><c:when test="${empty deliveries}"><div class="empty-state"><i class="bi bi-truck"></i><h3>No scheduled requests</h3><p>No pickup or delivery records match your filters.</p></div></c:when><c:otherwise><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Order</th><th>Customer</th><th>Pickup</th><th>Delivery</th><th>Coordinator</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody><c:forEach items="${deliveries}" var="delivery"><tr><td><strong><c:out value="${delivery.orderNumber}"/></strong></td><td><c:out value="${delivery.customerName}"/></td><td><c:out value="${empty delivery.pickupAt?'Not scheduled':delivery.pickupAt}"/></td><td><c:out value="${empty delivery.deliveryAt?'Not scheduled':delivery.deliveryAt}"/></td><td><c:out value="${empty delivery.coordinatorName?'Unassigned':delivery.coordinatorName}"/></td><td><span class="status-badge" data-status="${delivery.status}"><c:out value="${delivery.status}"/></span></td><td class="text-end"><div class="btn-group"><a class="btn btn-sm btn-light" href="${pageContext.request.contextPath}/deliveries/view?id=${delivery.id}"><i class="bi bi-eye"></i></a><c:if test="${sessionScope.role!='CUSTOMER'}"><a class="btn btn-sm btn-light" href="${pageContext.request.contextPath}/deliveries/edit?id=${delivery.id}"><i class="bi bi-pencil"></i></a></c:if></div></td></tr></c:forEach></tbody></table></div></c:otherwise></c:choose></div>
<jsp:include page="/WEB-INF/views/common/footer.jsp"/>
